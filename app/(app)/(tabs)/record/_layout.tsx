import {Stack} from "expo-router";
import React from "react";

export default function RecordLayout() {
    return (
        <Stack
            screenOptions={{
                headerShown: false,
            }}>
        </Stack>
    );
}
